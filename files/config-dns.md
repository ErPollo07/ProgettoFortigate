# Configurazione DNS

Per configurare il DNS bisogna accedere al pannello `Network` > `DNS`.
Da li inserire come primary dns il dns della nostra rete nella vlan 20.
Con secondary dns possiamo inserire un dns pubblico come `8.8.8.8`.
